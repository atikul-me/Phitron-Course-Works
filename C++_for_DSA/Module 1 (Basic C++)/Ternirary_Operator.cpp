#include <iostream>
using namespace std;
int main()
{
    int x;
    cin >> x;

    x%2==0 ? cout << "Even" : cout << "Odd"; // if-else simplify, only effected if no else if and single if_else statement
    
    return 0;
}