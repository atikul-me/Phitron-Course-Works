#include <stdio.h>
int main()
{
    int num;
    scanf("%d", &num);

    int sum = num % 2;

    if(sum = 0)
    {
        printf("%d is an Even Number", num);
    }

    else 
    {
        printf("%d is an Odd Number", num);
    }

    return 0;
}